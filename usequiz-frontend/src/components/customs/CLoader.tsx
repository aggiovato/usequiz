const CLoader = () => {
  return (
    <div className="w-full h-full flex items-center justify-center p-10">
      <div className="animate-spin rounded-full h-10 w-10 border-t-4 border-teal-600 border-opacity-50" />
    </div>
  );
};

export default CLoader;
